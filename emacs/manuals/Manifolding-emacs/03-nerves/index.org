#+title: Stage 03 — Nerves (reflexes)

Automatic responses: protection first (backups, auto-saving),
then the senses (character sets, syntax tables), then the first
true reflex (keyboard macros). Lifecycle order follows the
structure: guard memory, then feel text, then react without
thinking.

Members:
- =00-backups-and-auto-saving.org= — Backups and Auto-Saving
- =01-non-ascii-characters.org= — Non-ASCII Characters
- =02-syntax-tables.org= — Syntax Tables
- =03-keyboard-macros.org= — Keyboard Macros

Subdivision backlog: none yet.
