#+title: Stage 05 — World (interaction)

The organism reaches outward: shells first (command, then Eshell,
then VTerm), then file territory (dired, files, file handling),
then beyond the machine (hyperlinks, everywhere, credentials).
Lifecycle order follows the structure: dissolve the boundary, walk
the filesystem, then extend presence outward.

Members:
- =00-shell-commands.org= — Shell Commands
- =01-eshell.org= — Eshell
- =02-vterm.org= — VTerm
- =03-dired.org= — Dired
- =04-files.org= — Files
- =05-file-handling.org= — full title verbatim (holds the shadowing-files note)
- =06-hyperlinking-and-web-navigation.org= — full title verbatim (holds the nyxt note)
- =07-emacs-everywhere.org= — emacs-everywhere
- =08-persistent-authentication.org= — Persistent Authentication

Subdivision backlog: none yet.
