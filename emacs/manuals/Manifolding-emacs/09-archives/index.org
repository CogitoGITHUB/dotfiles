#+title: Stage 09 — Archives (retrieval)

Experience becomes durable: documentation, help, search rituals,
accumulated conventions — then the reference small-fry, each
flagged for cheap future re-cut, and session persistence last
(the archive that closes the lifecycle). Lifecycle order:
document → ask → search → conventions → reference → persist.

Members (verdict-locked first):
- =00-documentation.org= — Documentation
- =01-getting-help.org= — Getting Help
- =02-searching-and-matching.org= — Searching and Matching
- =03-searching-and-replacement.org= — Searching and Replacement
- =04-tips-and-conventions.org= — Tips and Conventions

Reference small-fry (placed here as retrievable reference; any of
these re-cuts elsewhere with a git mv + manifest line, no ceremony):
- =05-international-character-set-support.org= — charset tables
- =06-editing-commands.org= — command reference (~3k lines)
- =07-document-viewing.org= — DocView
- =08-parsing-program-source.org= — parsing reference
- =09-editing-binary-files.org= — hexl reference
- =10-gnu-hyperbole.org= — Hyperbole system
- =11-d-bus-integration.org= — D-Bus (world-adjacent; parked here pending call)
- =12-desktop-session-persistence.org= — session save/restore (lifecycle end)

Subdivision backlog: =06-editing-commands.org= may deserve rituals
or its own split later — flagged, undecided.
