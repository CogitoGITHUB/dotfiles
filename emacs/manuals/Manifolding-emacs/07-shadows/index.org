#+title: Stage 07 — Shadows (resilience)

When the system is failing or opaque: crashes, errors, debugging,
recovery. One territory — Dealing with Trouble already contains
debugging, error interpretation, and recovery as its level-2
structure, so no subdivision is needed or wanted. Lifecycle rule of
this stage: SHADOWS means the system is failing, ARCHIVES means you
are retrieving; never confuse the two.

Members:
- =00-dealing-with-trouble.org= — Dealing with Trouble (incl.
  Debugging Lisp Programs, Recovery After a Crash, Emergency
  Escape, all as level-2 structure)

Subdivision backlog: none — the section arrives pre-manifolded.
