#+manifold-source: ../manuals.org
#+manifold-lines: 39043-39059
#+manifold-stage: 06-forge
#+manifold-date: 2026-09-15

* org-supertag

#+begin_src emacs-lisp
  ;; leaf block for org-supertag
(leaf org-supertag
  :straight (org-supertag :type git :host github :repo "yibie/org-supertag")
  :config
  ;; directories to sync
  (setq org-supertag-sync-directories '("~/org/"))  ;; point to your org files
  ;; optional: initialize database on startup
  ;; (supertag-sync-full-initialize) ;; you can call this manually when needed
  )

#+end_src


