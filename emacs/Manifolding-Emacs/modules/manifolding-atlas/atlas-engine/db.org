(require 'cl-lib)
#+TITLE: Manifolding Atlas — Database
#+priority: 3

* Database

** Database Core
#+begin_src emacs-lisp
(provide 'manifolding-atlas-db)
(provide 'manifolding-atlas-db-query)

(defvar manifolding-atlas-db--connection nil
  "DB connection.")

(defun manifolding-atlas-db (&optional _args)
  "Return DB connection, opening it if needed."
  (require 'emacsql)
  (require 'emacsql-sqlite-builtin)
  (unless (and manifolding-atlas-db--connection
               (ignore-errors (emacsql-live-p manifolding-atlas-db--connection)))
    (setq manifolding-atlas-db--connection
          (emacsql-sqlite-builtin manifolding-atlas-db-location)))
  (when (and manifolding-atlas-db--connection
             (not (bound-and-true-p my/manifolding-atlas--notes-table-ready)))
    ;; Guarantee `notes' exists for every caller (find/query/create),
    ;; not just update-file path. Direct emacsql on connection avoids
    ;; recursing via ensure-notes-table -> (manifolding-atlas-db).
    (ignore-errors
      (emacsql manifolding-atlas-db--connection
               [:create :table :if :not :exists notes
                ([(id :text :primary-key :not-null)
                  (title :text)
                  (path :text :not-null)
                  (level :integer)
                  (pos :integer)
                  (unfoldings :text)
                  (tags :text)])]))
    (setq my/manifolding-atlas--notes-table-ready t))
  (when (and manifolding-atlas-db--connection
             (not (bound-and-true-p my/manifolding-atlas--links-table-ready)))
    (ignore-errors
      (emacsql manifolding-atlas-db--connection
               [:create :table :if :not :exists links
                ([(source :text :not-null)
                  (dest :text :not-null)]
                 (:primary-key [source dest]))]))
    (setq my/manifolding-atlas--links-table-ready t))
  (when (and manifolding-atlas-db--connection
             (fboundp 'my/wormhole-links-ensure-table)
             (not (bound-and-true-p my/manifolding-atlas--wormhole-links-table-ready)))
    ;; atlas-wormholes.org loads after db.org, so guard with fboundp —
    ;; symmetric with restoration.org's (when (fboundp ...)) call guard.
    (ignore-errors (my/wormhole-links-ensure-table))
    (setq my/manifolding-atlas--wormhole-links-table-ready t))
  manifolding-atlas-db--connection)

;; Missing core shims — extractor/parse-ctx structs, notes table, and workers
;; These are normally provided by the external `manifolding-atlas' package;
;; when that package is absent (as in this Termux checkout) we provide
;; minimal compatible implementations so `notes' actually populates.

(unless (fboundp 'make-manifolding-atlas-parse-ctx)
  (cl-defstruct (manifolding-atlas-parse-ctx (:constructor make-manifolding-atlas-parse-ctx))
    path ast file-node heading-nodes hash mtime size))

(unless (fboundp 'make-manifolding-atlas-extractor)
  (cl-defstruct (manifolding-atlas-extractor (:constructor make-manifolding-atlas-extractor))
    name version priority extract-fn requires-ast))

(defvar manifolding-atlas-db--extractors nil
  "List of registered `manifolding-atlas-extractor' structs, sorted by priority.")

(defun manifolding-atlas--register-extractor (extractor)
  "Register EXTRACTOR and keep `manifolding-atlas-db--extractors' sorted by :priority."
  (setq manifolding-atlas-db--extractors
        (sort (cons extractor
                    (cl-remove (plist-get (list :name (manifolding-atlas-extractor-name extractor)) :name)
                               manifolding-atlas-db--extractors
                               :key #'manifolding-atlas-extractor-name :test #'eq))
              (lambda (a b) (< (or (manifolding-atlas-extractor-priority a) 100)
                               (or (manifolding-atlas-extractor-priority b) 100)))))
  ;; de-duplicate by name keep highest priority (last wins, sort stable)
  (setq manifolding-atlas-db--extractors
        (cl-remove-duplicates manifolding-atlas-db--extractors :key #'manifolding-atlas-extractor-name :test #'eq :from-end t)))

(defun my/manifolding-atlas--ensure-notes-table ()
  "Create `notes' table if absent — mirrors `citation.org' pattern."
  (emacsql (manifolding-atlas-db)
           [:create :table :if :not :exists notes
            ([(id :text :primary-key :not-null)
              (title :text)
              (path :text :not-null)
              (level :integer)
              (pos :integer)
              (unfoldings :text)
              (tags :text)])]))

(defun my/manifolding-atlas--row-to-note (row)
  "Normalize DB ROW (vector or list) to plist note; tolerate nil/malformed `unfoldings'/`tags' via `ignore-errors' + `read'."
  (if (and (listp row) (plist-get row :id))
      row
    (let ((v (if (vectorp row) (append row nil) row)))
      (list :id (nth 0 v) :title (nth 1 v) :path (nth 2 v)
            :level (or (nth 3 v) 0) :pos (or (nth 4 v) 1)
            :unfoldings (ignore-errors (read (nth 5 v))) :tags (ignore-errors (read (nth 6 v)))
            :aliases nil :primary-title nil :meta nil))))

(defun my/manifolding-atlas--ensure-links-table ()
  "Create `links' table if absent — queried by guard, may not be created by extractors."
  (ignore-errors
    (emacsql (manifolding-atlas-db)
             [:create :table :if :not :exists links
              ([(source :text :not-null)
                (dest :text :not-null)]
               (:primary-key [source dest]))])))

(with-eval-after-load 'manifolding-atlas
  (my/manifolding-atlas--ensure-notes-table)
  (my/manifolding-atlas--ensure-links-table)
  (add-hook 'manifolding-atlas-db-autosync-mode-hook #'my/manifolding-atlas--ensure-notes-table)
  (add-hook 'manifolding-atlas-db-autosync-mode-hook #'my/manifolding-atlas--ensure-links-table))

(cl-defun manifolding-atlas-db--update-file (path)
  "Parse PATH, upsert its `notes' row, and run all registered extractors.
Returns 1 if a note was indexed, 0 otherwise. Handles deletion when file missing."
  (my/manifolding-atlas--ensure-notes-table)
  (if (not (file-exists-p path))
      (progn
        (ignore-errors (emacsql (manifolding-atlas-db) [:delete :from notes :where (= path $s1)] path))
        ;; also purge dependent tables via cascade or explicit
        (dolist (extractor manifolding-atlas-db--extractors)
          (ignore-errors (funcall (manifolding-atlas-extractor-extract-fn extractor)
                                  (make-manifolding-atlas-parse-ctx :path path :ast nil) nil)))
        0)
    ;; Build parse-ctx via existing override if available, else minimal
    (let* ((ctx (if (fboundp 'manifolding-atlas-db--parse-file)
                    (ignore-errors (manifolding-atlas-db--parse-file path))
                  (with-temp-buffer
                    (insert-file-contents path)
                    (let ((ast (ignore-errors (org-element-parse-buffer))))
                      (make-manifolding-atlas-parse-ctx :path path :ast ast)))))
           (ast (and ctx (manifolding-atlas-parse-ctx-ast ctx)))
            (id (with-temp-buffer
                  (insert-file-contents path)
                  (when (re-search-forward "^:ID:[ \t]+\\(.+\\)" nil t)
                    (string-trim (match-string 1)))))
           (title (with-temp-buffer
                    (insert-file-contents path)
                    (when (re-search-forward "^\\*+[ \t]+\\(.*\\)" nil t)
                      (string-trim (match-string 1))))))
      (unless id
        (ignore-errors (emacsql (manifolding-atlas-db) [:delete :from notes :where (= path $s1)] path))
        (cl-return-from manifolding-atlas-db--update-file 0))
      (let ((unfoldings (with-temp-buffer
                     (insert-file-contents path)
                     (when (re-search-forward "^:PROPERTIES:" nil t)
                       (let (alist)
                         (while (re-search-forward "^:\\([^:]+\\):[ \t]*\\(.*\\)" nil t)
                           (let ((k (match-string 1)))
                             (if (string= k "END") (goto-char (point-max))
                               (push (cons k (match-string 2)) alist))))
                         (nreverse alist)))))
            (tags (with-temp-buffer
                    (insert-file-contents path)
                    (when (re-search-forward "^\\*+[ \t]+.*:\\([^:\n]+\\):[ \t]*$" nil t)
                      (split-string (match-string 1) ":" t)))))
        ;; upsert notes row
        (ignore-errors (emacsql (manifolding-atlas-db) [:delete :from notes :where (= id $s1)] id))
        (emacsql (manifolding-atlas-db)
                 [:insert :into notes :values $v1]
                 (vector id (or title (file-name-nondirectory path)) path 0 1
                         (prin1-to-string unfoldings) (prin1-to-string tags)))
        ;; run extractors
        (let ((note-data (list :id id :title title :path path)))
          (dolist (extractor (sort (copy-sequence manifolding-atlas-db--extractors)
                                   (lambda (a b) (< (or (manifolding-atlas-extractor-priority a) 100)
                                                    (or (manifolding-atlas-extractor-priority b) 100)))))
            (let ((fn (manifolding-atlas-extractor-extract-fn extractor)))
              (when fn
                (ignore-errors
                  (let ((result (funcall fn ctx note-data)))
                    (when result (setq note-data result))))))))
        (if (car (emacsql (manifolding-atlas-db) [:select * :from notes :where (= id $s1)] id)) 1 0)))))

(defvar manifolding-atlas--autosync-mode nil)

(defun manifolding-atlas--autosync-mode (&optional arg)
  "Toggle autosync: on save under `manifolding-atlas-db-sync-directories', call `manifolding-atlas-db--update-file'."
  (setq manifolding-atlas--autosync-mode
        (if (null arg) (not manifolding-atlas--autosync-mode)
          (> (prefix-numeric-value arg) 0)))
  (if manifolding-atlas--autosync-mode
      (add-hook 'after-save-hook #'manifolding-atlas--autosync-after-save nil t)
    (remove-hook 'after-save-hook #'manifolding-atlas--autosync-after-save t)))

(defun manifolding-atlas--autosync-after-save ()
  (when (and buffer-file-name
             (cl-some (lambda (dir) (string-prefix-p (expand-file-name dir) (expand-file-name buffer-file-name)))
                      manifolding-atlas-db-sync-directories))
    (manifolding-atlas-db--update-file buffer-file-name)))
#+end_src

*** Orphan prune (state comparison, not hooks)
Shell rm, git checkout, and external tools never fire Emacs hooks,
so deleted files linger as DB ghosts (visible in find completion)
until something diffs. This does: every distinct DB path missing
on disk goes through `manifolding-atlas-db--update-file', which
already deletes the row. File existence is truth — same rule as
the blueprint `.known-keys' reconcile. Admin history untouched.
#+begin_src emacs-lisp
(defun my/manifolding-atlas-db--prune-orphans ()
  "Drop DB note rows for vault files that no longer exist on disk.
Returns pruned count. Never touches files or admin history.
Skips silently when the engine (emacsql/manifolding-atlas-db) is not
loaded yet — boot timers fire before it; the idle repeat catches up."
  (if (not (and (fboundp 'emacsql) (fboundp 'manifolding-atlas-db)
                (fboundp 'manifolding-atlas-db--update-file)))
      0
    (let ((pruned 0))
    (my/manifolding-atlas--ensure-notes-table)
    (let ((paths (ignore-errors
                   (mapcar #'car (emacsql (manifolding-atlas-db)
                                          [:select :distinct [path] :from notes])))))
      (dolist (p paths)
        (when (and (stringp p) (not (file-exists-p p)))
          (ignore-errors (manifolding-atlas-db--update-file p))
          (setq pruned (1+ pruned)))))
    (when (> pruned 0)
      (message "db orphans: pruned %d note(s) for deleted files" pruned))
    pruned)))

(defun manifolding-atlas-db-query (&optional filter)
  "Query notes, filtered by FILTER if given.
Rows whose file no longer exists on disk are dropped before FILTER
is applied, so callers never see stale/deleted entries."
  (let* ((db (ignore-errors (manifolding-atlas-db)))
         (db-result (when db
                      (let ((notes (seq-filter
                                    (lambda (n) (file-exists-p (manifolding-atlas-note-path n)))
                                    (mapcar #'my/manifolding-atlas--row-to-note (emacsql db [:select * :from notes])))))
                        (if filter (seq-filter filter notes) notes)))))
    (if (and db-result (consp db-result))
        db-result
      ;; Fallback: scan vault directly when DB not available or empty (same fallback as elsewhere tonight)
      (let* ((root (my/manifolding-atlas-root-dir))
             (files (ignore-errors (my/manifolding-atlas-db-sync--list-org-files root)))
             notes)
        (dolist (file (or files (directory-files-recursively root "^[^.]" t)))
          (when (and (file-regular-p file)
                     (not (string-match-p "/\\.git/" file))
                     (not (string-match-p "/admin/" file)))
            (let ((id (ignore-errors
                        (with-temp-buffer
                          (insert-file-contents file)
                          (when (re-search-forward "^:ID:[ \t]+\\(.+\\)" nil t)
                            (string-trim (match-string 1)))))))
              (when id
                (let ((title (ignore-errors
                               (with-temp-buffer
                                 (insert-file-contents file)
                                 (when (re-search-forward "^\\*+[ \t]+\\(.*\\)" nil t)
                                   (let ((raw (string-trim (match-string 1))))
                                     (if (string-match "^TODO[ \t]+\\(.*\\)" raw)
                                         (string-trim (match-string 1 raw))
                                       raw)))))))
                  (push (list :id id :title (or title (file-name-nondirectory file))
                              :path file :level 0 :pos 1
                              :tags nil :unfoldings nil :aliases nil :primary-title nil :meta nil)
                        notes))))))
        (if filter
            (seq-filter filter notes)
          notes)))))

(defun manifolding-atlas-db-query-by-unfolding (unfolding val)
  (let ((db (manifolding-atlas-db)))
    (if db
        (mapcar #'my/manifolding-atlas--row-to-note (emacsql db [:select * :from notes :where (= unfoldings $s1)] (format "\"%s\":\"%s\"" unfolding val)))
      ;; Fallback: scan vault and filter by unfolding (uses same vault-scan as db-query)
      (seq-filter (lambda (n)
                    (let ((unfoldings (manifolding-atlas-note-unfoldings n)))
                      (equal (cdr (assoc (upcase unfolding) unfoldings)) val)))
                  (ignore-errors (manifolding-atlas-db-query))))))

(defun manifolding-atlas-db-query-by-ids (ids)
  (let ((db (manifolding-atlas-db)))
    (if (and db ids)
        (mapcar #'my/manifolding-atlas--row-to-note (emacsql db [:select * :from notes :where (in id $v1)] (vconcat ids)))
      ;; Fallback: scan vault and filter by ids
      (when ids
        (seq-filter (lambda (n) (member (manifolding-atlas-note-id n) ids))
                    (ignore-errors (manifolding-atlas-db-query)))))))

(defun manifolding-atlas-db-query-by-file-path (path)
  (let ((db (manifolding-atlas-db)))
    (if db
        (mapcar #'my/manifolding-atlas--row-to-note (emacsql db [:select * :from notes :where (= path $s1)] path))
      ;; Fallback: scan vault and filter by path
      (seq-filter (lambda (n) (string-equal (manifolding-atlas-note-path n) path))
                  (ignore-errors (manifolding-atlas-db-query))))))

(defun manifolding-atlas-db-query-by-tags-some (tags)
  (let ((db (manifolding-atlas-db)))
    (if db
        (seq-filter (lambda (n) (seq-intersection tags (manifolding-atlas-note-tags n)))
                    (ignore-errors (manifolding-atlas-db-query)))
      ;; Fallback: same filter on vault scan when DB unavailable
      (seq-filter (lambda (n) (seq-intersection tags (manifolding-atlas-note-tags n)))
                  (ignore-errors (manifolding-atlas-db-query))))))

(defun manifolding-atlas-db-query-by-links-some (links)
  (let ((db (manifolding-atlas-db)))
    (if db
        (emacsql db [:select * :from links :where (in source $v1)] (vconcat (mapcar #'car links)))
      ;; Fallback: links require DB, vault scan has no link index
      (progn
        (message "manifolding-atlas-db-query-by-links-some: DB unavailable, links not indexed")
        nil))))

(defun manifolding-atlas-db-query-links-to (id)
  (let ((db (manifolding-atlas-db)))
    (if db
        (emacsql db [:select * :from links :where (= dest $s1)] id)
      (progn
        (message "manifolding-atlas-db-query-links-to: DB unavailable")
        nil))))

(defun manifolding-atlas-db-query-links-from (id)
  (let ((db (manifolding-atlas-db)))
    (if db
        (emacsql db [:select * :from links :where (= source $s1)] id)
      (progn
        (message "manifolding-atlas-db-query-links-from: DB unavailable")
        nil))))

(defun manifolding-atlas-db-get-by-id (id)
  (let ((db (manifolding-atlas-db)))
    (if db
        (or (car (mapcar #'my/manifolding-atlas--row-to-note (emacsql db [:select * :from notes :where (= id $s1)] id)))
            (user-error "manifolding-atlas-db-get-by-id: ID %s not found" id))
      (let ((file (condition-case nil (org-id-find-id-file id) (error nil))))
        (if (and file (file-exists-p file))
            (with-temp-buffer
              (insert-file-contents file)
              (let ((title (when (re-search-forward "^\\*+[ \t]+\\(.*\\)" nil t)
                             (let ((raw (string-trim (match-string 1))))
                               (if (string-match "^TODO[ \t]+\\(.*\\)" raw)
                                   (string-trim (match-string 1 raw))
                                 raw)))))
                (list :id id
                      :title (or title (file-name-nondirectory file))
                      :path file
                      :level 0 :pos 1
                      :tags nil :unfoldings nil
                      :aliases nil :primary-title nil
                      :meta nil)))
          (user-error "manifolding-atlas-db-get-by-id: ID %s not found" id))))))

(defun manifolding-atlas-db-update-file (path)
  (let ((db (manifolding-atlas-db)))
    (if db
        (or (ignore-errors (manifolding-atlas-db--update-file path)) 0)
      1)))

(defun manifolding-atlas-db-prune-missing ()
  "Remove DB rows whose file no longer exists on disk.
Safe to call anytime; only deletes rows, never touches files."
  (interactive)
  (let* ((db (ignore-errors (manifolding-atlas-db)))
         (rows (and db (ignore-errors (emacsql db [:select [id path] :from notes]))))
         (removed 0))
    (dolist (row rows)
      (let ((id (car row)) (path (cadr row)))
        (unless (file-exists-p path)
          (emacsql db [:delete :from notes :where (= id $s1)] id)
          (setq removed (1+ removed)))))
    (when (called-interactively-p 'any)
      (message "manifolding-atlas: pruned %d stale note(s)" removed))
    removed))

(add-hook 'emacs-startup-hook #'manifolding-atlas-db-prune-missing)

(defun my/manifolding-atlas--prune-on-delete-file (orig-fn filename &rest args)
  "After deleting FILENAME, prune it from the DB if it was a vault file."
  (prog1 (apply orig-fn filename args)
    (when (and (stringp filename)
               (fboundp 'my/manifolding-atlas-root-dir)
               (stringp (my/manifolding-atlas-root-dir))
               (string-prefix-p (expand-file-name (my/manifolding-atlas-root-dir))
                                 (expand-file-name filename)))
      (ignore-errors (manifolding-atlas-db-prune-missing)))))

(advice-add 'delete-file :around #'my/manifolding-atlas--prune-on-delete-file)

(defun manifolding-atlas-db-count-notes ()
  (let ((db (manifolding-atlas-db)))
    (if db
        (or (caar (emacsql db [:select (funcall count *) :from notes]))
            (length (ignore-errors (manifolding-atlas-db-query))))
      ;; Fallback: count vault files via same scan as db-query
      (length (ignore-errors (manifolding-atlas-db-query))))))

(defun manifolding-atlas-select-from (prompt candidates &rest _args)
  "Real select-from: completing-read over CANDIDATES titles, return existing or new plist."
  (require 'seq)
  (let* ((titles (mapcar (lambda (n) (or (manifolding-atlas-note-title n) (plist-get n :title) "")) candidates))
         (choice (completing-read (cond ((string-suffix-p ": " prompt) prompt)
                                         ((string-suffix-p ":" prompt) (concat prompt " "))
                                         (t (concat prompt ": ")))
                                   titles nil nil nil nil nil))
         (found (seq-find (lambda (n) (string= (or (manifolding-atlas-note-title n) (plist-get n :title) "") choice)) candidates)))
    (if found
        found
      (list :id nil :title choice :path nil :unfoldings nil :tags nil))))

(cl-defun manifolding-atlas-select (prompt &key require-match filter-fn candidates)
  "Select a note with PROMPT via completing-read.
CANDIDATES defaults to every note in the database (via
`manifolding-atlas-db-query'). FILTER-FN, if given, is applied to
narrow the auto-fetched candidates down first. REQUIRE-MATCH is
passed through to the underlying prompt."
  (let* ((cands (or candidates (ignore-errors (manifolding-atlas-db-query))))
         (cands (if filter-fn (seq-filter filter-fn cands) cands)))
    (manifolding-atlas-select-from prompt cands :require-match require-match)))

(defun manifolding-atlas-db-register-extractor (extractor)
  (when (fboundp 'manifolding-atlas--register-extractor)
    (ignore-errors (manifolding-atlas--register-extractor extractor))))

(defun manifolding-atlas-db-autosync-mode (&optional arg)
  (when (fboundp 'manifolding-atlas--autosync-mode)
    (ignore-errors (manifolding-atlas--autosync-mode arg))))

(defvar manifolding-atlas-db-location
  (expand-file-name "admin/manifolding-atlas.db"
                    (expand-file-name "test/" "~")))

(defvar manifolding-atlas-db-sync-directories
  (list (expand-file-name "test/" "~")))

(defvar my--orig-note-p (when (fboundp 'manifolding-atlas-note-p) (symbol-function 'manifolding-atlas-note-p)))
(defun manifolding-atlas-note-p (obj)
  (or (and (listp obj) (plist-get obj :id))
      (when (and obj my--orig-note-p) (ignore-errors (funcall my--orig-note-p obj)))))

(defvar my--orig-note-id (when (fboundp 'manifolding-atlas-note-id) (symbol-function 'manifolding-atlas-note-id)))
(defun manifolding-atlas-note-id (note)
  (or (plist-get note :id)
      (when (and note my--orig-note-id) (ignore-errors (funcall my--orig-note-id note)))))

(defvar my--orig-note-title (when (fboundp 'manifolding-atlas-note-title) (symbol-function 'manifolding-atlas-note-title)))
(defun manifolding-atlas-note-title (note)
  (or (plist-get note :title)
      (when (and note my--orig-note-title) (ignore-errors (funcall my--orig-note-title note)))))

(defvar my--orig-note-path (when (fboundp 'manifolding-atlas-note-path) (symbol-function 'manifolding-atlas-note-path)))
(defun manifolding-atlas-note-path (note)
  (or (plist-get note :path)
      (when (and note my--orig-note-path) (ignore-errors (funcall my--orig-note-path note)))))

(defvar my--orig-note-unfoldings (when (fboundp 'manifolding-atlas-note-unfoldings) (symbol-function 'manifolding-atlas-note-unfoldings)))
(defun manifolding-atlas-note-unfoldings (note)
  (or (plist-get note :unfoldings)
      (when (and note my--orig-note-unfoldings) (ignore-errors (funcall my--orig-note-unfoldings note)))))

(defvar my--orig-note-tags (when (fboundp 'manifolding-atlas-note-tags) (symbol-function 'manifolding-atlas-note-tags)))
(defun manifolding-atlas-note-tags (note)
  (or (plist-get note :tags)
      (plist-get note :tag)
      (when (and note my--orig-note-tags) (ignore-errors (funcall my--orig-note-tags note)))))

(defvar my--orig-note-meta-get (when (fboundp 'manifolding-atlas-note-meta-get) (symbol-function 'manifolding-atlas-note-meta-get)))
(defun manifolding-atlas-note-meta-get (note key type)
  (or (let ((v (plist-get note :meta)))
        (when v
          (let ((pair (assoc key v)))
            (when pair (cdr pair)))))
      (when (and note my--orig-note-meta-get) (ignore-errors (funcall my--orig-note-meta-get note key type)))))

(defvar my--orig-note-meta-get-list (when (fboundp 'manifolding-atlas-note-meta-get-list) (symbol-function 'manifolding-atlas-note-meta-get-list)))
(defun manifolding-atlas-note-meta-get-list (note key type)
  (or (let ((v (plist-get note :meta)))
        (when v
          (let ((vals (cdr (assoc key v))))
            (when vals (if (listp vals) vals (list vals))))))
      (when (and note my--orig-note-meta-get-list) (ignore-errors (funcall my--orig-note-meta-get-list note key type)))))

(defvar my--orig-note-level (when (fboundp 'manifolding-atlas-note-level) (symbol-function 'manifolding-atlas-note-level)))
(defun manifolding-atlas-note-level (note)
  (or (plist-get note :level)
      (when (and note my--orig-note-level) (ignore-errors (funcall my--orig-note-level note)))))

(defvar my--orig-note-pos (when (fboundp 'manifolding-atlas-note-pos) (symbol-function 'manifolding-atlas-note-pos)))
(defun manifolding-atlas-note-pos (note)
  (or (plist-get note :pos)
      (when (and note my--orig-note-pos) (ignore-errors (funcall my--orig-note-pos note)))))

(defvar my--orig-note-aliases (when (fboundp 'manifolding-atlas-note-aliases) (symbol-function 'manifolding-atlas-note-aliases)))
(defun manifolding-atlas-note-aliases (note)
  (or (plist-get note :aliases)
      (when (and note my--orig-note-aliases) (ignore-errors (funcall my--orig-note-aliases note)))))

(defvar my--orig-note-primary-title (when (fboundp 'manifolding-atlas-note-primary-title) (symbol-function 'manifolding-atlas-note-primary-title)))
(defun manifolding-atlas-note-primary-title (note)
  (or (plist-get note :primary-title)
      (when (and note my--orig-note-primary-title) (ignore-errors (funcall my--orig-note-primary-title note)))))

(defvar my--orig-note-outline-path (when (fboundp 'manifolding-atlas-note-outline-path) (symbol-function 'manifolding-atlas-note-outline-path)))
(defun manifolding-atlas-note-outline-path (note)
  (or (plist-get note :outline-path)
      (when (and note my--orig-note-outline-path) (ignore-errors (funcall my--orig-note-outline-path note)))))

(defvar my--orig-links-current-note-id (when (fboundp 'my/manifolding-atlas-links--current-note-id) (symbol-function 'my/manifolding-atlas-links--current-note-id)))
(defun my/manifolding-atlas-links--current-note-id ()
  (or (when (and (fboundp 'my/manifolding-atlas--note-at-point) (ignore-errors (my/manifolding-atlas--note-at-point)))
        (ignore-errors (manifolding-atlas-note-id (my/manifolding-atlas--note-at-point))))
      (when (and (fboundp 'my/manifolding-atlas--buffer-note) (ignore-errors (my/manifolding-atlas--buffer-note)))
        (ignore-errors (manifolding-atlas-note-id (my/manifolding-atlas--buffer-note))))
      (when my--orig-links-current-note-id (ignore-errors (funcall my--orig-links-current-note-id)))))


#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Extraction
#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
(defun manifolding-atlas-db--extract-aliases (unfoldings)
  "Extract aliases from UNFOLDINGS alist.

Looks for unfolding defined by `manifolding-atlas-buffer-alias-unfolding'.
Handles both quoted aliases (with spaces) and unquoted aliases properly.
Strips org links and emphasis markers from each alias.

The unfolding name is upcased before lookup because UNFOLDINGS keys
are stored upcased, so a lowercase or mixed-case
`manifolding-atlas-buffer-alias-unfolding' still matches."
  (when-let* ((aliases-str (cdr (assoc (upcase manifolding-atlas-buffer-alias-unfolding)
                                       unfoldings))))
    (setq aliases-str (string-trim aliases-str))
    (let ((result nil)
          (pos 0))
      (while (< pos (length aliases-str))
        ;; Skip whitespace
        (while (and (< pos (length aliases-str))
                    (= (aref aliases-str pos) ? ))
          (setq pos (1+ pos)))
        (when (< pos (length aliases-str))
          (let ((char (aref aliases-str pos)))
            (cond
             ;; Quoted alias - find matching closing quote
             ((= char ?\")
              (let ((end (string-match "\"" aliases-str (1+ pos))))
                (if end
                    (progn
                      (push (manifolding-atlas-db--strip-emphasis
                             (org-link-display-format
                              (substring aliases-str (1+ pos) end)))
                            result)
                      (setq pos (1+ end)))
                  (error "Unmatched quote in %s" manifolding-atlas-buffer-alias-unfolding))))
             ;; Unquoted alias - find next space or end of string
             (t
              (let ((end (or (string-match " " aliases-str pos)
                             (length aliases-str))))
                (push (manifolding-atlas-db--strip-emphasis
                       (org-link-display-format
                        (substring aliases-str pos end)))
                       result)
                (setq pos end)))))))
      (nreverse result))))


#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Query
#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Worker
#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Sync
#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Mentions Detection

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Metadata Manipulation

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Utilities

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Tags Manipulation

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Note Selection

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src


** Note Structure

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

#+begin_src emacs-lisp
#+end_src

