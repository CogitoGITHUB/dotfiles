#+title: manuals.org — pointer stub

This file held 86 manual sections (7,022,771 bytes). All of them were
CUT — never copied — into =manuals/Manifolding-emacs/= across stages
00–09 plus =grimoire-unsorted= (including a 4-line preamble note of
unknown authorship about touch-typing and org-drill, now
=grimoire-unsorted/preamble-notes.org=).

Nothing remains here by design. The append-only move log lives in
=manuals/index.org=; each destination carries its source line range
in a provenance header. To verify: sum(moved content) +
consumed separators == 7,022,771.
