{ config, pkgs, inputs, noctalia, ... }:

{
  # Required by home-manager
  home.username = "asdf";
  home.homeDirectory = "/home/asdf";
  home.stateVersion = "25.05";  # lock to current era; don't change casually

  imports = [
    inputs.noctalia.homeModules.default
  ];

  programs.noctalia-shell = {
    enable = true;

    settings = {
      bar = {
        density = "compact";
        position = "right";
      };
    };
  };
}
