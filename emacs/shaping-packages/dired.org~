* Dired — The Filesystem Blade
:PROPERTIES:
:PACKAGE:  dired
:BUILT-IN: t
:END:

** Base Configuration :init:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § DIRED CORE SETTINGS — POWER & ELEGANCE
;; ════════════════════════════════════════════════════════════
(setq dired-listing-switches "-alh --group-directories-first"
      dired-dwim-target t                    ; Smart target for copy/move
      dired-recursive-copies 'always         ; Copy directories recursively
      dired-recursive-deletes 'always        ; Delete directories recursively
      dired-kill-when-opening-new-dired-buffer t  ; Clean up old buffers
      delete-by-moving-to-trash t)           ; Safety net
#+end_src

** Load Dired :config:
#+begin_src emacs-lisp
(require 'dired)
(require 'dired-x)  ; Extra dired functionality
#+end_src

** Auto-Revert in Dired :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § AUTO-REFRESH — LIVING FILESYSTEM VIEW
;; ════════════════════════════════════════════════════════════
(add-hook 'dired-mode-hook #'auto-revert-mode)
(setq auto-revert-verbose nil
      auto-revert-interval 1)
#+end_src

** Hide Details by Default :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § VISUAL CLARITY — CLEAN BY DEFAULT
;; ════════════════════════════════════════════════════════════
(add-hook 'dired-mode-hook #'dired-hide-details-mode)
#+end_src

** Omit Unwanted Files :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § NOISE REDUCTION — FOCUS ON WHAT MATTERS
;; ════════════════════════════════════════════════════════════
(setq dired-omit-files
      (concat dired-omit-files
              "\\|^\\.DS_Store$"
              "\\|^\\.git$"
              "\\|^\\.gitignore$"
              "\\|^__pycache__$"
              "\\|^\\.mypy_cache$"
              "\\|\\.pyc$"
              "\\|^node_modules$"
              "\\|^\\.next$"
              "\\|^\\.cache$"))

(add-hook 'dired-mode-hook #'dired-omit-mode)
#+end_src

** Image Previews :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § IMAGE THUMBNAILS — VISUAL FEEDBACK
;; ════════════════════════════════════════════════════════════
(setq image-dired-thumb-size 128
      image-dired-thumb-margin 5)
#+end_src

** File Preview System :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § PREVIEW WINDOW — SEE BEFORE YOU OPEN
;; ════════════════════════════════════════════════════════════
(defvar shapeshifter/dired-preview-window nil
  "Window used for file previews.")

(defvar shapeshifter/dired-preview-buffer nil
  "Buffer used for file previews.")

(defun shapeshifter/dired-preview-file ()
  "Preview file at point in a side window."
  (interactive)
  (let ((file (dired-get-filename nil t)))
    (when file
      (if (file-directory-p file)
          (message "Cannot preview directory: %s" file)
        ;; Close existing preview if any
        (when (and shapeshifter/dired-preview-window
                   (window-live-p shapeshifter/dired-preview-window))
          (delete-window shapeshifter/dired-preview-window))
        
        ;; Create preview window
        (setq shapeshifter/dired-preview-window
              (display-buffer-in-side-window
               (find-file-noselect file)
               '((side . right) (window-width . 0.5))))
        
        (with-selected-window shapeshifter/dired-preview-window
          (setq shapeshifter/dired-preview-buffer (current-buffer))
          (read-only-mode 1))))))

(defun shapeshifter/dired-preview-close ()
  "Close the preview window."
  (interactive)
  (when (and shapeshifter/dired-preview-window
             (window-live-p shapeshifter/dired-preview-window))
    (delete-window shapeshifter/dired-preview-window))
  (when (and shapeshifter/dired-preview-buffer
             (buffer-live-p shapeshifter/dired-preview-buffer))
    (kill-buffer shapeshifter/dired-preview-buffer))
  (setq shapeshifter/dired-preview-window nil
        shapeshifter/dired-preview-buffer nil))

(defun shapeshifter/dired-toggle-preview-mode ()
  "Toggle automatic preview on navigation."
  (interactive)
  (if (bound-and-true-p shapeshifter/dired-auto-preview)
      (progn
        (setq shapeshifter/dired-auto-preview nil)
        (shapeshifter/dired-preview-close)
        (message "Auto-preview disabled"))
    (setq shapeshifter/dired-auto-preview t)
    (message "Auto-preview enabled")))
#+end_src

** Enhanced Open :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § SMART OPEN — CONTEXT-AWARE FILE HANDLING
;; ════════════════════════════════════════════════════════════
(defun shapeshifter/dired-enhanced-open ()
  "Open file with context awareness.
For images: show in image-mode
For directories: enter them
For text: open normally"
  (interactive)
  (let ((file (dired-get-filename nil t)))
    (cond
     ((file-directory-p file)
      (dired-find-file))
     ((string-match-p "\\.\\(jpg\\|jpeg\\|png\\|gif\\|bmp\\|svg\\)\\'" file)
      (find-file file)
      (image-mode))
     ((string-match-p "\\.\\(mp4\\|mkv\\|avi\\|mov\\)\\'" file)
      (start-process "mpv" nil "mpv" file)
      (message "Opening %s with mpv..." (file-name-nondirectory file)))
     ((string-match-p "\\.pdf\\'" file)
      (start-process "pdf-viewer" nil "xdg-open" file)
      (message "Opening %s with external viewer..." (file-name-nondirectory file)))
     (t
      (dired-find-file)))))
#+end_src

** File Operations :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § FILE OPERATIONS — SURGICAL PRECISION
;; ════════════════════════════════════════════════════════════
(defun shapeshifter/dired-create-file ()
  "Create a new file in current directory."
  (interactive)
  (let ((filename (read-string "Create file: ")))
    (when (> (length filename) 0)
      (write-region "" nil filename)
      (revert-buffer)
      (dired-goto-file filename))))

(defun shapeshifter/dired-open-as-root ()
  "Open dired as root using sudo/doas."
  (interactive)
  (let ((sudo-cmd (if (executable-find "doas") "doas" "sudo")))
    (dired (concat "/" sudo-cmd "::" default-directory))))

(defun shapeshifter/dired-kill-other-dired-buffers ()
  "Kill all dired buffers except current one."
  (interactive)
  (let ((current (current-buffer)))
    (dolist (buf (buffer-list))
      (with-current-buffer buf
        (when (and (eq major-mode 'dired-mode)
                   (not (eq buf current)))
          (kill-buffer buf)))))
  (message "Killed all other dired buffers"))
#+end_src

** Sorting Functions :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § SORTING — ORDER FROM CHAOS
;; ════════════════════════════════════════════════════════════
(defun shapeshifter/dired-sort-by-size ()
  "Sort dired buffer by file size."
  (interactive)
  (dired-sort-other (concat dired-listing-switches "S"))
  (message "Sorted by size"))

(defun shapeshifter/dired-sort-by-time ()
  "Sort dired buffer by modification time."
  (interactive)
  (dired-sort-other (concat dired-listing-switches "t"))
  (message "Sorted by time"))

(defun shapeshifter/dired-sort-by-extension ()
  "Sort dired buffer by file extension."
  (interactive)
  (dired-sort-other (concat dired-listing-switches "X"))
  (message "Sorted by extension"))

(defun shapeshifter/dired-sort-by-name ()
  "Sort dired buffer by name (default)."
  (interactive)
  (dired-sort-other dired-listing-switches)
  (message "Sorted by name"))
#+end_src

** Metadata & Analysis :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § METADATA — KNOW YOUR FILES
;; ════════════════════════════════════════════════════════════
(defun shapeshifter/dired-file-info ()
  "Show detailed file information."
  (interactive)
  (let* ((file (dired-get-filename))
         (attrs (file-attributes file))
         (size (file-size-human-readable (file-attribute-size attrs)))
         (mtime (format-time-string "%Y-%m-%d %H:%M:%S" 
                                   (file-attribute-modification-time attrs)))
         (perms (file-attribute-modes attrs)))
    (message "File: %s | Size: %s | Modified: %s | Perms: %s"
             (file-name-nondirectory file) size mtime perms)))

(defun shapeshifter/dired-directory-summary ()
  "Show summary statistics for current directory."
  (interactive)
  (let ((files 0)
        (dirs 0)
        (total-size 0))
    (dolist (file (directory-files default-directory t "^[^.]"))
      (if (file-directory-p file)
          (setq dirs (1+ dirs))
        (setq files (1+ files)
              total-size (+ total-size (file-attribute-size 
                                       (file-attributes file))))))
    (message "Directory: %d files, %d dirs, %s total"
             files dirs (file-size-human-readable total-size))))

(defun shapeshifter/dired-count-marked ()
  "Count and show marked files."
  (interactive)
  (let ((count (length (dired-get-marked-files))))
    (message "%d file%s marked" count (if (= count 1) "" "s"))))
#+end_src

** Quick Actions :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § QUICK ACTIONS — SPEED & EFFICIENCY
;; ════════════════════════════════════════════════════════════
(defun shapeshifter/dired-open-in-external-app ()
  "Open file with system default application."
  (interactive)
  (let ((file (dired-get-filename nil t)))
    (call-process "xdg-open" nil 0 nil file)
    (message "Opened %s externally" (file-name-nondirectory file))))

(defun shapeshifter/dired-copy-path ()
  "Copy full path of file at point."
  (interactive)
  (let ((path (dired-get-filename)))
    (kill-new path)
    (message "Copied: %s" path)))

(defun shapeshifter/dired-copy-filename-only ()
  "Copy just the filename without path."
  (interactive)
  (let ((filename (file-name-nondirectory (dired-get-filename))))
    (kill-new filename)
    (message "Copied: %s" filename)))
#+end_src

** Mode Line Enhancement :config:
#+begin_src emacs-lisp
;; ════════════════════════════════════════════════════════════
;; § STATUS DISPLAY — KNOW YOUR CONTEXT
;; ════════════════════════════════════════════════════════════
(add-hook 'dired-mode-hook
          (lambda ()
            (setq mode-line-format
                  (list
                   " "
                   'mode-line-buffer-identification
                   " | "
                   '(:eval (format "%d files" 
                                   (length (directory-files default-directory nil "^[^.]"))))
                   " | "
                   '(:eval (if (bound-and-true-p shapeshifter/dired-auto-preview)
                               "PREVIEW"
                             ""))
                   " %l"))))
#+end_src
