;; Home User Packages and Services

(define-module (home packages-services)
  #:use-module (gnu packages))

(define-public literativeos-home-packages
  (list))

(define-public literativeos-home-services
  (list))
