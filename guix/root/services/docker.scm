;;; Docker service configuration placeholder
(define-public literativeos-docker-service-type
  (service-type
   (name 'literativeos-docker)
   (extensions '())
   (default-value #f)
   (description "Docker service (not configured)")))
